# import os.path as osp

# BASE_PATH = osp.dirname(osp.abspath(__file__).split("asl")[0])

# DATASET_PATH = osp.join(BASE_PATH, "data" ,"dataset")

# GCN_DATASET_PATH = osp.join(DATASET_PATH, "gcn", "dataset")